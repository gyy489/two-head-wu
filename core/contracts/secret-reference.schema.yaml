$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "https://two-head-wu.local/contracts/secret-reference.schema.yaml"
title: "Two-Headed-Wu Secret Reference"
type: string
pattern: "^secret://[a-z0-9-]+/[a-z0-9-]+/[a-z0-9-]+$"
description: "Logical secret reference only. A secret value, location, username, or key path is never represented here."
